apiVersion: apps/v1
kind: Deployment
metadata:
  name: sanos-usuarios
  namespace: sanosysalvos
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sanos-usuarios
  template:
    metadata:
      labels:
        app: sanos-usuarios
    spec:
      containers:
        - name: usuarios
          image: 053760945279.dkr.ecr.us-east-1.amazonaws.com/sanos-usuarios-serv:v2
          ports:
            - containerPort: 8000
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: sanos-secrets
                  key: DATABASE_URL_USUARIOS
            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: sanos-secrets
                  key: SECRET_KEY_USUARIOS
            - name: DEBUG
              value: "False"
            - name: ALLOWED_HOSTS
              value: "*"
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: sanos-usuarios
  namespace: sanosysalvos
spec:
  selector:
    app: sanos-usuarios
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
